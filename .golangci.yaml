# Our default golangci-lint configuration
# If these linters fail, then it should fail the build.
version: "2"
run:
  timeout: 5m
linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
